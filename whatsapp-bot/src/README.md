# WhatsApp Bot 模块化结构说明

## 📁 文件结构

```
whatsapp-bot/
├── src/
│   ├── config.js              # 配置管理
│   ├── utils.js               # 工具函数
│   ├── sessionManager.js      # 会话管理器
│   ├── laravel.js             # Laravel API 调用
│   ├── WhatsAppSession.js    # WhatsApp 会话类
│   └── routes.js              # Express 路由
├── server.js                  # 主入口文件
└── ...
```

## 📝 模块说明

### config.js
- 集中管理所有配置项
- 端口、Laravel URL、会话目录等

### utils.js
- 工具函数集合
- JID 转手机号、群组 ID 格式化、会话文件删除等

### sessionManager.js
- 会话存储和管理
- 机器人删除状态管理
- 提供统一的会话访问接口

### laravel.js
- Laravel API 调用封装
- 所有与 Laravel 后端的通信
- 支持依赖注入（机器人删除检查、删除处理）

### WhatsAppSession.js
- WhatsApp 会话类
- 连接管理、事件监听、重连逻辑
- 群组和成员事件处理

### routes.js
- Express 路由定义
- API 端点处理
- 支持依赖注入（重连回调）

### server.js
- 应用入口
- 初始化所有模块
- 服务器启动和优雅关闭

## 🔄 模块依赖关系

```
server.js
    ├── config.js
    ├── utils.js
    ├── sessionManager.js
    ├── laravel.js (依赖注入: sessionManager)
    ├── WhatsAppSession.js (依赖注入: isBotDeleted, reconnectCallback)
    └── routes.js (依赖注入: reconnectCallback)
```

## ✨ 优势

1. **模块化**：每个模块职责单一，易于维护
2. **可测试性**：模块独立，便于单元测试
3. **可扩展性**：新增功能只需修改对应模块
4. **依赖注入**：通过参数传递依赖，降低耦合度
5. **代码复用**：工具函数和配置集中管理

